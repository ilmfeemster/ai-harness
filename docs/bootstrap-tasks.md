# Bootstrap Tasks

> **Harness-development-only artifact:** This file temporarily tracks AI Development Harness work before GitHub Issues are established. It must not be included in projects created from the reusable template.

Delete this file after Phase 0.3 transfers all remaining work into GitHub Issues.

## Status values

- Ready
- In progress
- Blocked
- Complete — awaiting approval
- Complete

## Phase 0.1 — Establish the harness repository

**Status:** Complete

Created the repository constitution and core project documents.

## Phase 0.2 — Define document responsibilities and reuse boundaries

**Status:** Complete — awaiting approval

### Goal

Define where each kind of information belongs and distinguish:

- reusable workflow assets;
- reusable document scaffolds;
- project-specific intelligence and state;
- harness-development-only bootstrap artifacts.

### Exit condition

The reusable document responsibility matrix lives in `AGENTS.md`, project-context documents are clearly identified, and `docs/architecture.md` contains only harness architecture rather than generic workflow governance.

## Phase 0.3 — Define the GitHub Issue workflow

**Status:** Ready

### Goal

Create Issue templates, readiness rules, minimal labels, and the Issue-to-slice promotion workflow.

### Exit condition

The remaining Phase 0 work is represented by GitHub Issues and this temporary file can be deleted.

## Phase 0.4 — Define the slice standard

**Status:** Pending

Finalize the reusable bounded execution-package schema while keeping each active slice project-specific.

## Phase 0.5 — Define planning modes

**Status:** Pending

Formalize context loading and output expectations for each workflow operation.

## Phase 0.6 — Add lightweight local validation

**Status:** Pending

Add structural checks for required files, headings, references, validation commands, unresolved placeholders, and template-context leakage.

## Phase 0.7 — Add project-start instructions

**Status:** Pending

Define how to copy reusable assets, initialize clean project documents, and avoid inheriting harness-specific context.

## Phase 0.8 — Compare against the fantasy workflow

**Status:** Pending

Bring over proven workflow rules without copying fantasy-specific content.

## Phase 0.9 — Run a real fantasy-app slice

**Status:** Pending

Exercise Issue → slice → implementation → validation → review on a real work item.

## Phase 0.10 — Stabilize Phase 0

**Status:** Pending

Correct friction found through real use and declare Phase 0 complete.
